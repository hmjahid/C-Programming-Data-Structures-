#include <stdio.h>
#define VALUE 89 //Using capital letters is a good practice.It's reduce the chance of error.

int main() {
     int value = 74;
      printf("%d", value);
        return 0;
        }
