#include <stdio.h>

int main ()   {

          int var = 0X43ff ; // In variablr decleartiom small letters and capital letters make same senses
          printf ("%x" , var);
          printf ("%X", var);
           // Output will be varied by small x and Capital X in printf function
          printf ("%d", var); // Converts the value to decimal value
          return 0;

          }
